%
% This is part of BLFSbookja package.
%
% This is a CTIE change file for the original XML source of the BLFSbook.
%
% $Author$
% $Rev$
% $Date::                           $
%
@x
<?xml version="1.0" encoding="ISO-8859-1"?>
@y
<?xml version="1.0" encoding="UTF-8"?>
@z

@x
    <para><parameter>--localstatedir=/var/lib</parameter>: This parameter is
    used so that all <application>ScrollKeeper</application> files are
    installed in, and the <application>ScrollKeeper</application> database is
    properly updated in
    <filename class="directory">/var/lib/scrollkeeper</filename> instead of
    some files being installed in
    <filename class="directory">$GNOME_PREFIX/var/scrollkeeper</filename>.</para>
@y
    <para>
    <parameter>--localstatedir=/var/lib</parameter>:
    このパラメーターは <application>ScrollKeeper</application> ファイル、および <application>ScrollKeeper</application> データベースの配置ディレクトリを指示するものであり、<filename
    class="directory">$GNOME_PREFIX/var/scrollkeeper</filename> ではなく <filename
    class="directory">/var/lib/scrollkeeper</filename> に設定するものです。
    </para>
@z
